# Desktop (WSL2 + CUDA): training
# Install torch FIRST, matching your CUDA version:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.2
transformers>=4.46
datasets>=2.19
accelerate>=0.30
peft>=0.11
evaluate>=0.4
bitsandbytes>=0.43
faster-whisper>=1.0
